// Problem-13
// Codeforces 219158/problem/K

#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int a , b , c;
    cin >> a >> b >> c;
    int d = max({a,b,c});
    int e = min({a,b,c});
    cout << e << ' ' << d << '\n';
}

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}
