// Problem-22
// Codeforces - 219432/problem/L
#include <bits/stdc++.h>
using namespace std;
void gcda(int a, int b){
    int result = gcd(a,b);
    cout << result << '\n';
}

void solve()
{
    int x,y;
    cin >> x >> y;
    gcda(x,y);
   
}

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}
