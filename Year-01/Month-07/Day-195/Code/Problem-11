// Problem-11
// Codeforces - 219432/problem/E

#include <bits/stdc++.h>
using namespace std;


void solve(){
    int n;
    cin >> n;
    vector <long long > d(n);
    for(int i = 0 ; i < n; i++){
        cin >> d[i];
    }
    sort(d.begin(),d.end());
    cout << d[n-1] << '\n';
    

}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    solve();

    return 0;
}
