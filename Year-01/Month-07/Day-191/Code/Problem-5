// Problem-5
// Codeforces 1791/C

#include <bits/stdc++.h>
using namespace std;

void solve(){
   
    int n;
    cin >> n;
    string s;
    cin >> s;
    while(((s[0] == '1' && s.back() == '0') || (s[0] == '0' && s.back() == '1'))){
        
            s.erase(0,1);
            s.pop_back();
        
    }
    cout << s.size() << '\n';
    


}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    int t;
    cin >> t;
    while(t--){
    solve();
    }
    

    return 0;
}
