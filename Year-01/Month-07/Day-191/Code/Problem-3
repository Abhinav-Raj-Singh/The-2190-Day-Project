// Problem-3
// Codeforces 1760/A

#include <bits/stdc++.h>
using namespace std;

void solve(){
    vector <int> d(3);
    for(int i = 0 ; i < 3 ; i++){
        cin >> d[i];
    }
    sort(d.begin(),d.end());
    cout << d[1] << '\n';

}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    int t;
    cin >> t;
    while(t--){
    solve();
    }

    return 0;
}
