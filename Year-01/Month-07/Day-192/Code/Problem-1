// Problem-1 
// Codeforces 451/A

#include <bits/stdc++.h>
using namespace std;

void solve(){
   
    int n, m;
    cin >> n >> m;
    cout << (min(n,m) % 2 == 0 ? "Malvika" : "Akshat") << '\n';


}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    // int t;
    // cin >> t;
    // while(t--){
    solve();
    // }
    

    return 0;
}
