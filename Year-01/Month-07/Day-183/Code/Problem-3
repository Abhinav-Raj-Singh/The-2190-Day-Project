// Problem-3
// Codeforces - 133 A

#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    cin >> s;
    if(s.find('H') != string::npos || s.find('Q') != string::npos || s.find('9') != string::npos){
        cout << "YES" << '\n';
    }
    else{
        cout << "NO" << '\n';
        
    }
}

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}
