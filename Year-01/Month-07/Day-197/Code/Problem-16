// Problem-16
// Codeforces - 219774/problem/K

#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n;
    cin >> n;
  string s;
  cin >> s;
  int sum = 0 ;
  for(int i = 0 ; i < n; i++){
        sum += (s[i]-'0');
  }
  cout << sum << '\n';
   
    
}

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    solve();
    
    
    return 0;
}
