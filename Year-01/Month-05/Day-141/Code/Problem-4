// 288A

#include <bits/stdc++.h>
using namespace std;

void solve(){

    int n = 4 ; 
    
    long long a;
    set <int> b;
    for(size_t i = 0 ; i < n ; i++){
        cin >> a;
        b.insert(a);
    }
    cout << (n-(b.size())) << '\n';

}


int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    solve();

    return 0; 

}
