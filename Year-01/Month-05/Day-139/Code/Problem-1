// 271A

#include <bits/stdc++.h>
using namespace std;

void solve(){
    int year;
    cin >> year;
    int temp_year = year;
    int ans = year;


    
    for(int i = 0 ; i < 100000 ; i++){
        set <int> numbers;
        for(int k = 0 ; k < 4 ; k++){
        numbers.insert(temp_year%10);
        temp_year = temp_year / 10;
        }
        if((numbers.size() == 4) && (i>0)){
            cout << ans << '\n';
            break;
        }   
        if((numbers.size() != 4)){
        temp_year = year + i;
        ans = year + i;
    }
        
    }
}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);

    solve();

    return 0;
}
