// 2254A

#include <bits/stdc++.h>
using namespace std;

void solve (){
    int a , b , c ;
    cin >> a >> b >> c;
    vector <int> d = {a,b,c};
    int rounds = 0 ;
    if(a == b || b == c || c == a){
        
        }
    else{
    sort(d.begin() , d.end());
    // int medium = d[1];
    int maximum = max({a,b,c});
    // rounds = maximum - medium;
    int minimum = min({a,b,c});

    for(int i = 0 ; i <= 10 ; i++){
        d[2]--;
        d[0]++ ;
        rounds++ ;
        if(d[0] == d[1] || d[1] == d[2] || d[2] == d[0]){
            break;
        }
        
    }
}


// Check answer is what 
    
      
    cout << rounds << '\n';

}


int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);

    int t ;
    cin >> t;
    while(t--){
        solve();
    }

    return 0;


}
