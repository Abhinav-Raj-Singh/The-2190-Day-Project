// 443A

#include <bits/stdc++.h>
using namespace std;

void solve(){
        string n;
        getline(cin , n);
        set <char> d ;
      
        for(int i = 1; i < (n.size()-1) ; i++){
            d.insert(n[i]);
            i += 2;
        }
        cout << d.size() << '\n';
    }


int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    solve();
    return 0;


}
