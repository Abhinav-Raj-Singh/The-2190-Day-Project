// 520A

#include <bits/stdc++.h>
using namespace std;

void solve(){

    int n;
    cin >> n;
    string s;
    cin >> s;
    set <char> a;
    string check = "NO";
    for(int i = 0 ; i < n ; i++){
        s[i] = tolower(s[i]);
    }
    for(int i = 0 ; i < n ; i++){
        a.insert(s[i]);
    }
    if(a.size() == 26){
        cout << "YES" << '\n';
    }
    else{
        cout << "NO" << '\n';
    }

}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);

    solve();
    

    return 0;
}
