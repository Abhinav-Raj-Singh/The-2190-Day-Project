1. `1LL << a` : is used to calculate integer powers of 2 (safely up to a = 61 or 62 for 
   a 64-bit `long long`), avoiding the precision loss and floating-point inaccuracies 
   that occur with `pow(2, a)` for larger values.
