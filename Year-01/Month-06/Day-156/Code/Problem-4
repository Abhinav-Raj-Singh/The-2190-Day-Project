// Codeforces 1915A

#include <bits/stdc++.h> 
using namespace std;


#ifndef ONLINE_JUDGE
#define debug(x) cerr << #x << " " << x << '\n'
#else 
#define debug(x)
#endif

void solve(){
    // set <int> d;
    int a , b , c;
    cin >> a >> b >> c;
    
    
    // cout << d[0] << '\n'; 
    if(a == b){
        cout << c << '\n';
    }
    else if(b == c){
        cout << a << '\n';

    }
    else {
        cout << b<< '\n';

    }
}

int main(){
	#ifndef ONLINE_JUDGE
		freopen("Error.txt" , "w" , stderr);
	#endif

ios_base::sync_with_stdio(0);cin.tie(0);
	
	
int t; 
cin >> t;
while(t--){
solve();
}

return 0;

}
