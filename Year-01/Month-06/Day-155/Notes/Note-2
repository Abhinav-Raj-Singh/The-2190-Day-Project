		

6. `static_assert` : C++ also has another type of assert called `static_assert`. A **static_assert** 
    is an assertion that is checked at compile-time rather than at runtime, with a failing `static_assert`
    causing a compile error. Unlike assert, which is declared in the `<cassert>` header, static_assert 
    is a keyword, so no header needs to be included to use it. A `static_assert` takes the following form:
    `static_assert(condition, diagnostic_message)` If the condition is not true, the diagnostic message is 
    printed.
		1. Because `static_assert` is evaluated by the compiler, the condition must be a constant expression.
		2. `static_assert` can be placed anywhere in the code file (even in the global namespace).
		3. `static_assert` is not deactivated in release builds (like normal `assert` is).
		4.  Because the compiler does the evaluation, there is no runtime cost to a `static_assert`.
		5. `Best practice` : Favor `static_assert` over `assert()` whenever possible.		
		
		
7. `Asserts vs error handling` : `Tip` :
			1.  Use assertions to detect programming errors, incorrect assumptions, or conditions that 
			should never occur in correct code. It is the programmer’s responsibility to fix these, 
			so we want to catch them early.
			2. Use error handling for issues that we expect will occur during the normal operation of your
			program.
			3. Use both in cases where something isn’t supposed to occur, but we want to fail gracefully 
			if it does.
			
			
8. `Some assert limitations and warnings` : 
			1. First, the assert itself can be improperly written. If this happens, the assert will
			either report an error where none exists, or fail to report a bug where one does exist.
			2. Second, your `assert()` expressions should have no side effects, as the assert expression 
			won’t be evaluated when `NDEBUG` is defined (and thus the side effect won’t be applied).
			Otherwise, what you are testing in a debug configuration will not be the same as in a 
			release configuration (assuming you ship with NDEBUG).
			3. Also note that the `abort()` function terminates the program immediately, without
			a chance to do any further cleanup (e.g. close a file or database). Because of this, 
			asserts should be used only in cases where corruption isn’t likely to occur if the program 
			terminates unexpectedly.
			

9. You can't read `vector <string> word;` fully by `cin >> word;` you have to use loop as you did in standard vectors.

10. `vector <string> words(3);` --> it is necessary to define size of a vector string if you wanted to operate on indices.

11. `vector <string> word;` --> elements indiced from a string vector will return us string and not character.

12. `String is also indixed by default` : but it returns character as elements.

13. `swap()` : we can use this for swapping strings as well as first character of strings as `swap(a[0] , b[0]);` -->
     this first character of both strings will get swapped. 
     
14. `a.substr(1); a.substr(1,2); ` --> first will make substring of string a from index 1 to last possible 
     index wherease second will make substring of 2 characters starting from index 1.
     
