// 2094 A

#include <bits/stdc++.h>
using namespace std;

void solve(){
    string a;
    string b;
    string c;
    cin >> a >> b >> c;
    cout << a[0] << b[0] << c[0] <<'\n';
}

int main(){

    ios_base::sync_with_stdio(0);cin.tie(0);
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}
