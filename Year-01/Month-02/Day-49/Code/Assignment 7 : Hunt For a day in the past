// Assignment 7 : Hunt for a day in the past

/*For example if today is a 3 [Wednesday] and we want to go 10 days in the past, we would hit a Sunday. The program should also do some basic error checking in that it wouldn't for example accept 8 as the current day. Only days from 1 to 7 are acceptable.
*/

#include <iostream>

int main(){

    int today;
    int days_passed;
    int past_day_number;
    
    std::cout << "Which day is today [1 : Monday ,,,, 7 : Sunday] : " << std::endl;
    std::cin >> today;
    std::cout << "How many days have passed up to today : " << std::endl;
    std::cin >> days_passed;

    if ( (today < 1) || (today > 7) ) {
        std::cout << today << " is not a valid Day. Bye!! " << std::endl;
        return 0;

    }

    if (days_passed < 0){
        std::cout << days_passed << "Is not a valid number please enter a positive number" << std::endl;
        return 0;
    }

    
    switch(today){

        case 1 : 
        std::cout << "Today is Monday" << std::endl;
        break;

        case 2 : 
        std::cout << "Today is Tuesday" << std::endl;
        break;

        case 3 : 
        std::cout << "Today is Wednesday" << std::endl;
        break;
        
        case 4 : 
        std::cout << "Today is Thursday" << std::endl;
        break;

        case 5 : 
        std::cout << "Today is Friday" << std::endl;
        break;

        case 6 : 
        std::cout << "Today is Saturday" << std::endl;
        break;

        case 7 : 
        std::cout << "Today is Sunday" << std::endl;
        break;
    }

    
    past_day_number = ( today - (days_passed % 7) + 7 ) % 7;


        switch(past_day_number){



        case 0 :
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Sunday" << std::endl;    
        break;

        case 6 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Saturday" << std::endl;
        break;

        case 5 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Friday" << std::endl;
        break;

        case 4 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Thursday" << std::endl;
        break;
        
        case 3 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Wednesday" << std::endl;
        break;

        case 2 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Tuesday" << std::endl;
        break;

        case 1 : 
        std:: cout << "if we went " << days_passed << " days in the past we would hit a Monday" << std::endl;
        break;
    }

    

    return 0;
}
